<UserControl 
  x:Class="GitKeeper.Main.Views.RepositoryPanel"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  xmlns:local="clr-namespace:GitKeeper.Main.Views"
  xmlns:prism="http://prismlibrary.com/"
  mc:Ignorable="d" 
  xmlns:controls="clr-namespace:GitKeeper.Controls;assembly=GitKeeper.Controls"

  d:DesignHeight="300" 
  d:DesignWidth="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
      <DataGrid Grid.Row="0" ItemsSource="{Binding CommitLogs}" />
      <DataGrid Grid.Row="1" ItemsSource="{Binding TreeEntrys}" />
  </Grid>
</UserControl>


